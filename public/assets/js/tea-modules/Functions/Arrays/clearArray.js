/**
 * @function
 *
 * @description Implements array cleanup.
 *
 * @param { Array } array
 *
 * @return { any[] }
 **/
export function clearArray(array)
{
    return array.splice(0, array.length);
}
