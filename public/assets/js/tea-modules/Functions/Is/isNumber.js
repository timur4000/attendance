/**
 * @function
 *
 * @description Checks if the given argument is a number.
 *
 * @param { any } verifiable
 *
 * @return { boolean }
 **/
export function isNumber(verifiable)
{
    return typeof verifiable === 'number';
}
