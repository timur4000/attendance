/**
 * @function
 *
 * @description Checks if the given argument is an array.
 *
 * @param { any } verifiable
 *
 * @return { boolean }
 **/
export function isArray(verifiable)
{
    return Array.isArray(verifiable);
}
